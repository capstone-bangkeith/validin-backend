generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model KodeWilayah {
  kodewilayah   String  @id @unique
  provinsi      String
  kabupatenkota String
  kecamatan     String
}
