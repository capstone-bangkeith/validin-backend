generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model kodewilayah {
  kodewilayah   String  @id
  provinsi      String
  kabupatenkota String
  kecamatan     String
}
